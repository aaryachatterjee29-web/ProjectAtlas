# frontend

Frontend application for Project Atlas.

Will hold the user-facing interface (e.g. dashboards, strategy builders,
backtest explorers, portfolio visualizations) that consumes the backend
API. Technology choices and structure will be defined in a later phase.
