# backend/database

Persistence layer for the platform.

Will contain ORM models, schema definitions, migrations, and session
management for storing strategy definitions, backtest results, portfolios,
and user/workspace metadata.
