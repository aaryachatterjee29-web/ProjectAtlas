# backend/api

HTTP API layer for the platform.

Will contain route definitions, request/response schemas, dependency wiring,
and any middleware. Intended to expose backend capabilities (data queries,
backtest runs, analytics) to the frontend and external clients.
