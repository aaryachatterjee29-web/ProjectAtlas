# backend/config

Application configuration management.

Will hold settings schemas, environment-specific configuration loaders,
and defaults used across the backend. The goal is a single, typed source of
truth for runtime configuration.
