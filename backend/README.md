# backend

Core Python package for the Project Atlas backend.

This package will house the data, research, analytics, backtesting, and
machine learning components of the platform, as well as the API layer that
exposes them. The entry point is `backend/main.py`.
