# backend/strategies

Trading strategy definitions and reusable components.

Will contain strategy base classes, signal composition utilities, and
individual strategy implementations that can be plugged into the
backtesting and live execution layers.
