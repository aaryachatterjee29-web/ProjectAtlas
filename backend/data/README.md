# backend/data

Market data ingestion, normalization, and access.

Will contain connectors to external data providers, raw and cleaned data
schemas, caching utilities, and the abstractions the rest of the platform
uses to query historical and reference data.
